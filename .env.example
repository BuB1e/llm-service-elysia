LLM_PROVIDER=groq
GROQ_API_KEY=
GROQ_MODEL=llama-3.1-8b-instant

# LLM Fallback Options
OLLAMA_URL=http://127.0.0.1:1234
OLLAMA_MODEL=meta-llama-3.1-8b-instruct
OPENAI_MODEL=gpt-oss
OPENAI_API_KEY=

# Production Config
# Comma-separated list of allowed origins (use * for all during development)
ALLOWED_ORIGINS=*
# Comma-separated list of valid API keys (leave empty to disable auth during development)
API_KEYS=
# Requests per minute per client
RATE_LIMIT_RPM=30
